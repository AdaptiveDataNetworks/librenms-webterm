composer.lock
/dist/
.DS_Store
.env
/gateway/bin/
/node_modules/
/.phpstan.cache/
/.phpunit.cache/
.phpunit.result.cache
*.secret
/site/
/vendor/

# Generated by tools/bundle-installer.sh at release time.
/build/

# The packages.adaptivedatanetworks.com landing page. Deployed straight to the
# host and kept out of the repository deliberately: it is site content, not part
# of the plugin, and an unfinished draft of it got committed once already.
/packaging/repo/index.html
