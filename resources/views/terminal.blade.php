{{-- The full-page terminal, reached from /plugin/WebTerm?device=N.

     Kept for direct links and for anyone who wants the terminal without the
     surrounding device page. The device tab is the normal way in, and both
     render the same partial so they cannot diverge. --}}
@include('WebTerm::partials.terminal')
